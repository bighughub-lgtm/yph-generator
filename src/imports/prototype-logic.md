Convert this existing Mockup Generator design into a fully functional interactive prototype.

IMPORTANT:
Do NOT change any visual design.
Do NOT change layout, spacing, typography, colors, sizes, images, or text.
Keep all frames, elements, dimensions and styling exactly as they are.

Only add functionality and interactive logic.

Add:

Step navigation logic:

Product → Brand → Quantity → Designs → Choose mockups → Finished Files

Enable "Continue" only after required selection is made

"Back" navigates to previous step without resetting data

Selection logic:

Product cards: single select (active state already styled)

Brand selection: single select

Quantity selection: single select

Mockups: multi-select with active state

Upload functionality (prototype logic only):

Clicking upload placeholders simulates image preview state

Allow up to 5 images

Enable drag-to-swap behavior (prototype level)

Remove button clears image

Validation:

Disable Continue if no required selection

Show error state if no design uploaded

Generate button:

Simulate loading state

Show Finished Files screen

Simulate progress indicator

Finished Files:

Download button triggers success state

"Generate new" resets flow to first screen

Use variables or component states where possible.
Keep everything visually identical.
Only enhance interactivity and state logic.

Do not redesign anything.